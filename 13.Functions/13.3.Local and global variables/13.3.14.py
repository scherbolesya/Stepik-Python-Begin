Выберите верные утверждения.
# Choose the correct statements.

# после выхода из функции локальные переменные станут недоступны
# локальные переменные, определенные в пределах функции, действуют только в ней
# функция вызывается по своему имени с указанием необходимых аргументов
# глобальные переменные доступны для всех функций

# after exiting the function, local variables will no longer be available
# local variables defined within a function have effect only within that function
# the function is called by its name with the required arguments
# global variables are available for all functions
