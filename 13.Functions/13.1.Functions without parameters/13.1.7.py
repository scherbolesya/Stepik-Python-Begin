Для чего используется оператор pass?
# What is the pass operator used for?
В ходе исполнения оператора pass ничего не происходит, поэтому он может использоваться в качестве заглушки в тех местах, где это синтаксически необходимо, 
например, в инструкциях, где тело является обязательным, таких как def. Таким образом, оператор pass используется там, где код пока ещё не появился, но планируется.
# Nothing happens while the pass statement is executed, so it can be used as a placeholder in places where it is syntactically necessary.
# for example, in statements where the body is mandatory, such as def. Thus, the pass operator is used where the code has not yet appeared, but is planned.

# для создания заглушки
# to create a stub
